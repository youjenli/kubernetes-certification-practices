# https://github.com/dgkanatsios/CKAD-exercises/blob/main/g.state.md
# Create a busybox pod with 'sleep 3600' as arguments.
# Copy '/etc/passwd' from the pod to your local folder
# k run busybox --image=busybox --restart=Never -- /bin/sh -c "sleep 3600"
# k cp busybox:etc/passwd passwd
# 可以在 minikube 環境使用。後面目的地的路徑是以命令列工作目錄為錨點。在 windows 上以「/」分隔路徑而非「\」。
