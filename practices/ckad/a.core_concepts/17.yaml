# https://github.com/dgkanatsios/CKAD-exercises/blob/main/a.core_concepts.md
# Create an nginx pod and set an env value as 'var1=val1'.
# Check the env value existence within the pod
# k run env-demo --image=nginx --env "var1=val1" -it --rm --restart=Never -- env
# 或是 k run nginx-demo --image=nginx --env=var1=val1 --restart=Never -it --rm -- env
