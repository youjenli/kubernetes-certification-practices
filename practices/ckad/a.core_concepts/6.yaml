# apiVersion: v1
# kind: List
# items:
# - apiVersion: v1
#   kind: ResourceQuota
#   metadata:
#     name: myrq
#   spec:
#     hard:
#       requests.cpu: "1"
#       requests.memory: 1Gi
#       limits.cpu: "1"
#       limits.memory: 1Gi

# - apiVersion: v1
#   kind: Pod

# kubectl create quota myrq --hard=cpu=1,memory=1G,pods=2 --dry-run=client -o yaml
