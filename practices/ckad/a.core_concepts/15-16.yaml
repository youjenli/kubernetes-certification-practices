# https://github.com/dgkanatsios/CKAD-exercises/blob/main/a.core_concepts.md
# Create a busybox pod that echoes 'hello world' and then exits
# k run busybox --image=busybox -it --restart=Never -- echo "hello world"
# 註：若沒加 -it 的話，本地的命令列會看不到上面指令輸出的內容

# Do the same, but have the pod deleted automatically when it's completed
# k run busybox --image=busybox -it --restart=Never --rm -- echo "hello world"
